import ChartProps from 'components/Chart/types';
import TableProps from 'components/Table/types';

export interface Props {
  /**
   * Chart's data
   */
  chartProps: ChartProps;
  /**
   * Table's list of informations
   */
  tableProps: TableProps;
}
